import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['assets2.drugcarts.com', 'assets1.drugcarts.com', 'assets3.drugcarts.com'],
  },
};

export default nextConfig;
